import React from 'react'; // Library for the UI
import ReactDOM from 'react-dom'; // Package for DOM-specific methods
import './index.css'; // CSS file design
import App from './App'; // App file

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

